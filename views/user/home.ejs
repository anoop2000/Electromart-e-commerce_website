<%- include("../../views/partials/user/header") %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css">

<style>
  .btn {
      display: inline-block;
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      color: #fff;
      background-color: #ff6b6b;
      border: none;
      border-radius: 5px;
      transition: background-color 0.3s, transform 0.3s;
      cursor: pointer;
      text-align: center;
  }


  .btn:hover {
      background-color: #ff4c4c;
      transform: translateY(-2px);
  }


  .btn:active {
      transform: translateY(1px);
  }
  .wishlist-btn {
    position: absolute;
    top: 50%; /* Vertically center within the container */
    right: -8px; /* Align to the right end */
    transform: translateY(-50%); /* Center align vertically */
    color: #17a2b8; /* Initial color (btn-info) */
    font-size: 15px; /* Set a smaller font size */
    z-index: 10;
    transition: color 0.3s, transform 0.3s;
    background: none; /* No background */
    border: none; /* No border */
    outline: none; /* Remove focus outline */
}

.wishlist-btn:hover {
    color: #eb0d0d; /* Change to btn-success on hover */
    transform: translateY(-50%) scale(1.1); /* Slight hover effect */
}

.wishlist-btn:active {
    transform: translateY(-50%) scale(0.95); /* Pressed effect */
}



  .product-img-action-wrap {
      position: relative;
  }


  .product-img {
      position: relative;
      display: block;
      overflow: hidden;
  }


  .product-cart-wrap {
      border: 1px solid #eee;
      padding: 20px;
      margin-bottom: 30px;
  }


  .product-content-wrap h6 {
      font-size: 18px;
      color: #333;
      margin-bottom: 5px;
  }


  .product-content-wrap h5 {
      font-size: 16px;
      color: #ff6b6b;
      margin-bottom: 10px;
  }


  .product-content-wrap a {
      display: inline-block;
      padding: 8px 12px;
      background-color: #ff6b6b;
      color: white;
      text-transform: uppercase;
      font-size: 14px;
      border-radius: 4px;
      text-decoration: none;
  }


  .product-content-wrap a:hover {
      background-color: #ff4c4c;
  }


  #modal-message {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: #333;
    color: white;
    padding: 10px 20px;
    border-radius: 5px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
    font-size: 14px;
    display: none; /* Initially hidden */
    z-index: 1000; /* Ensure it's on top */
    opacity: 0; /* Hidden via opacity */
    transition: opacity 0.3s ease-in-out; /* Smooth fade-in/out */
  }

  #modal-message.visible {
    display: block; /* Make visible */
    opacity: 1; /* Fully visible */
  }

  #modal-message.hidden {
    opacity: 0; /* Fade-out effect */
    transition: opacity 0.3s ease-in-out;
  }




  /* General Reset */
img {
    max-width:80%;
    height: auto;
    display: block;
}

/* Hero Slider Section */
.hero-slider .single-slider-img img {
    width: 980px; /* Full width of the container */
    height: 402px; /* Maintain aspect ratio */
    
    object-fit: cover; /* Ensure the image fills the area without distortion */
}

.hero-slider .swiper-container {
    width: 100%; /* Full-width container */
    max-width: 1200px; /* Limit the maximum width */
    margin: 0 auto; /* Center the slider */
}

.hero-slider .swiper-pagination,
.hero-slider .swiper-button-next,
.hero-slider .swiper-button-prev {
    /* Add styles for navigation elements if needed */
}

/* Banner Section */
.banner-area .single-banner img {
    width: 100%; /* Full width of the column */
    height: auto; /* Maintain aspect ratio */
    max-height: 300px; /* Limit banner height */
    object-fit: cover; /* Crop image gracefully if dimensions vary */
}

.banner-area .col-lg-4 {
    margin-bottom: 15px; /* Add spacing between banners */
}

.banner-area {
    text-align: center; /* Center-align banners */
    max-width: 1200px; /* Limit overall section width */
    margin: 0 auto; /* Center the content */
}




.bannerBox {
    padding: 10px; /* Space between the box and the image */
    border: 2px solid #ccc; /* Light gray border for the box */
    border-radius: 8px; /* Rounded corners */
    background-color: #f9f9f9; /* Subtle background color */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Optional shadow for a lifted effect */
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth hover effects */

}

.bannerBox:hover {
    transform: scale(1.05); /* Slight zoom effect on hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Enhanced shadow on hover */
}

.single-banner img {
    border-radius: 6px; /* Ensure the image corners align with the box */
}










.banner-title {
    font-size: 2rem;
    margin: 0;
    font-weight: bold;
}

.banner-description {
    font-size: 1rem;
    margin: 0;
    margin-top: 5px;
}









/* Media Queries for Responsiveness */
@media (max-width: 768px) {
    .hero-slider .single-slider-img img {
        max-height: 300px; /* Adjust slider height for smaller screens */
    }

    .banner-area .single-banner img {
        max-height: 200px; /* Adjust banner height for smaller screens */
    }
}

@media (max-width: 576px) {
    .banner-area .col-lg-4,
    .banner-area .col-md-6 {
        flex: 0 0 100%; /* Stack banners on small screens */
        max-width: 100%;
    }
}


    
  
</style>




<!-- Hero Slider Section -->
<!-- Replace your existing hero section HTML -->
<section class="hero-slider">
    <!-- Full-width container -->
    <div class="banner-container">
        <div class="swiper-container hero-swiper">
            <div class="swiper-wrapper">
                <% banner.slice(0, 2).forEach(bannerItem => { %>
                    <div class="swiper-slide">
                        <!-- Single Slide -->
                        <div class="single-slider">
                            <div class="single-slider-img">
                                <img src="/uploads/re-image/<%= bannerItem.image %>" 
                                     alt="<%= bannerItem.title %>">
                            </div>
                            <!-- Title and Description below the image -->
                            <div class="banner-content">
                                <h4 class="banner-title"><%= bannerItem.title %></h4>
                                <p class="banner-description"><%= bannerItem.description %></p>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
            <!-- Navigation arrows inside -->
            <div class="swiper-button-next"></div>
            <div class="swiper-button-prev"></div>
        </div>
    </div>
    
    <!-- Pagination dots outside -->
    <div class="swiper-pagination"></div>
</section>


<!-- Banner Section -->
<section class="banner-area pt-5 pb-5">
    <div class="container">
        <div class="row">
            <% banner.slice(2).forEach(bannerItem => { %>
                <div class="col-lg-4 col-md-6 bannerBox">
                    <div class="single-banner">
                        <a href="<%= bannerItem.link %>">
                            <img src="/uploads/re-image/<%= bannerItem.image %>" alt="<%= bannerItem.title %>" style="width: 100%; height: auto;">
                        </a>
                        <h3><%= bannerItem.title%></h3>
                        <p><%= bannerItem.description%></p>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
  </section>







  



<section class="product spad">
  <div class="container">
      <div class="row">
          <div class="col-lg-12">
              <ul class="filter__controls">
                  <li data-filter=".new-arrivals">New Arrivals</li>
              </ul>
          </div>
      </div>
      <div class="row product__filter">
        <%for(let i=0;i<products.length;i++){%>
              <div class="col-lg-3 col-md-4 col-12 col-sm-6">
                  <div class="product-cart-wrap mb-30">
                      <div class="product-img-action-wrap">
                          <div class="product-img product-img-zoom">

                              <a href="/product-details?id=<%=products[i]._id%>">
                                  <img class="default-img"
                                   src="/uploads/re-image/<%=products[i].productImage[0]%>" 
                                   alt="<%=products[i].productName%>">
                              </a>
                          </div>
                          <!-- <a href="/addT" class="wishlist-btn">
                              <i class="fas fa-heart"></i>
                          </a> -->
                      </div>

                      <!-- <div class="product-action-1" style="position: relative;" data-product-id="<%= products[i]._id %>">
                        <a aria-label="Add to Wishlist" class="wishlist-btn" href="/wishlist">
                            <i class="fas fa-heart"></i>
                        </a>
                       </div> -->

                       
                    
                    


                      <div class="product-content-wrap">
                          <h6><%=products[i].productName%></h6>
                          <h5><i class="fas fa-indian-rupee-sign"></i><%=products[i].salePrice%></h5>
                          <!-- <a href="/addToCart?id=<%=products[i]._id%>">Add to Cart</a> -->

                         
                          

                      </div>
                  </div>
              </div>
              <%}%>
      </div>
  </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    document.querySelectorAll(".wishlist-btn").forEach(button => {
      button.addEventListener("click", async (event) => {
        event.preventDefault(); 

        const productId = button.getAttribute("data-product-id");

        try {
          const response = await fetch('/addToWishlist', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ productId })
          });

          const result = await response.json();

          if (result.status) {
            Swal.fire({
              icon: 'success',
              title: 'Success',
              text: result.message,
            });
          } else {
            Swal.fire({
              icon: 'info',
              title: 'Oops!',
              text: result.message,
            });
          }
        } catch (error) {
          Swal.fire({
            icon: 'error',
            title: 'Error',
            text: 'Something went wrong! Please try again later.',
          });
        }
      });
    });
  });

  </script>

<script src="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.js"></script>


<script>
    document.addEventListener('DOMContentLoaded', function() {
        const heroSwiper = new Swiper('.hero-swiper', {
            // Enable loop mode
            loop: true,
            
            // Enable auto play
            autoplay: {
                delay: 3000,
                disableOnInteraction: false,
            },
            
            // Add effects
            effect: 'fade',
            fadeEffect: {
                crossFade: true
            },
            
            // Configure navigation
            navigation: {
                nextEl: '.swiper-button-next',
                prevEl: '.swiper-button-prev',
            },
            
            // Configure pagination
            pagination: {
                el: '.swiper-pagination',
                clickable: true,
            },
            
            // Responsive breakpoints
            breakpoints: {
                320: {
                    slidesPerView: 1
                },
                768: {
                    slidesPerView: 1
                },
                1024: {
                    slidesPerView: 1
                },
            },
            
            // Additional options
            speed: 1000,
            grabCursor: true,
            watchOverflow: true,
            centeredSlides: true,
            
            // Events
            on: {
                init: function () {
                    //console.log('Swiper initialized');
                },
            }
        });
    });
    </script>


    
    
    <!-- Add these styles -->
    <style>
        /* Full-width banner styles */
        .hero-slider {
            width: 100%;
            margin: 0;
            padding: 0;
        }
        
        .banner-container {
            width: 100%;
            position: relative;
            overflow: hidden;
        }
        
        /* Swiper container styles */
        .hero-swiper {
            width: 100%;
        }
        
        .single-slider-img {
            width: 100%;
            height: auto;
        }
        
        .single-slider-img img {
            width: 100%;
            height: auto;
            max-height: 777px;
            object-fit: cover;
        }
        
        /* Navigation arrows inside image */
        .swiper-button-next,
        .swiper-button-prev {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 50%;
            color: #000;
            z-index: 10;
        }
        
        .swiper-button-next {
            right: 20px;
        }
        
        .swiper-button-prev {
            left: 20px;
        }
        
        .swiper-button-next:hover,
        .swiper-button-prev:hover {
            background: rgba(255, 255, 255, 0.8);
        }
        
        /* Content container outside carousel */
        .banner-content-container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 0 15px;
            text-align: center;
        }
        
        .banner-content {
            margin-bottom: 20px;
        }
        
        .banner-title {
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 1rem;
        }
        
        .banner-description {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 20px;
        }
        
        /* Pagination styles - outside carousel */
        .swiper-pagination {
            position: static !important;
            margin-top: 15px;
        }
        
        .swiper-pagination-bullet {
            width: 12px;
            height: 12px;
            background: #333;
            opacity: 0.5;
            margin: 0 5px;
        }
        
        .swiper-pagination-bullet-active {
            opacity: 1;
            background: #000;
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            .banner-title {
                font-size: 1.8rem;
            }
            
            .banner-description {
                font-size: 1rem;
            }
        
            .swiper-button-next,
            .swiper-button-prev {
                width: 30px;
                height: 30px;
            }
        }
        </style>


<%- include("../../views/partials/user/footer") %>





  














